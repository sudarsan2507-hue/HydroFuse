# ---------------------------------------------------------------------------
# Python 3.11
# Install with:  py -3.11 -m venv .venv && .venv\Scripts\pip install -r requirements.txt
# ---------------------------------------------------------------------------

# --- Core scientific stack -------------------------------------------------
numpy>=1.26
pandas>=2.1
scipy>=1.11
matplotlib>=3.8
pyarrow>=14.0            # parquet writer for data/satellite.parquet

# --- Configuration ---------------------------------------------------------
PyYAML>=6.0              # reads config.yaml

# --- Phase 2: backend ------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.27  # ASGI server that actually runs the FastAPI app
pydantic>=2.6
SQLAlchemy>=2.0
psycopg2-binary>=2.9     # Postgres driver, used only when DATABASE_URL points at Postgres (e.g. hosted deploys)
httpx>=0.27              # used by the fake-payload poster and FastAPI TestClient

# --- Phase 4: satellite (Google Earth Engine) ------------------------------
# Free to use, but requires a Google account and a registered GEE cloud project.
# The pipeline falls back to a synthetic satellite frame if these are missing.
earthengine-api>=0.1.390
geemap>=0.32

# --- Phase 6: model --------------------------------------------------------
scikit-learn>=1.4
xgboost>=2.0
shap>=0.44
joblib>=1.3

# --- Phase 8: mapping ------------------------------------------------------
folium>=0.15

# --- Testing ---------------------------------------------------------------
pytest>=8.0
